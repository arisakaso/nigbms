_target_: nigbms.modules.data.OfflineDataModule
data_dir: /workspaces/nigbms/data/raw/poisson2d/sample
dataset_sizes: 
  train: 30
  val: 20
  test: 10
rtol_dists:
  train:
    _target_: nigbms.utils.distributions.NumpyConstant
    shape: null
    value: 1.0e-6
  val: ${.train}
  test: ${.train}
maxiter_dists:
  train: 
    _target_: nigbms.utils.distributions.NumpyConstant
    shape: null
    value: 1000
  val: ${.train}
  test: ${.train}
task_type:
  _target_: hydra.utils.get_class
  path: nigbms.modules.tasks.PETScLinearSystemTask
batch_size: 4
num_workers: 0